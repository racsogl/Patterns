/**
 * El objetivo del patrón Adapter es convertir la interfaz de una clase existente
 * en la interfaz esperada por los clientes también existentes de modo que
 * puedan trabajar de manera conjunta.
 * <p>
 * Se trata de conferir a una clase existente una nueva interfaz para responder
 * a las necesidades de los clientes.
 * Permite que cooperen clases que de otra forma no podrían por tener interfaces incompatibles.
 * <p>
 * Dominios:
 * Para integrar en el sistema un objeto cuya interfaz no se corresponde con a interfaz requerida
 * en el interior del sistema.
 * <p>
 * Para proveer interfaces múltiples a un objeto en su etapa de diseño.
 */
package com.racsogl.patterns.estructuracion.adapter;